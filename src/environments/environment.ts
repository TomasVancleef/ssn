// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBrL0NZizIUnIZKsiQHwtIw1ni1dVeRCSU",
    authDomain: "cosmo-chat-bf694.firebaseapp.com",
    databaseURL: "https://cosmo-chat-bf694.firebaseio.com",
    projectId: "cosmo-chat-bf694",
    storageBucket: "cosmo-chat-bf694.appspot.com",
    messagingSenderId: "649104050438",
    appId: "1:649104050438:web:73dd939565c17e5b127687"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
